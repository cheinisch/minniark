<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8">
  <title>{{ title }}</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 text-gray-900">

  <!-- ✅ Fixed NavBar -->
  <nav class="bg-white shadow sticky top-0 z-50">
    <div class="max-w-6xl mx-auto px-4 py-3 flex justify-between items-center">
      <span class="text-xl font-bold">📷 Image Portfolio</span>
      <ul class="flex space-x-4">
        <li><a href="/image_portfolio/home" class="text-gray-700 hover:text-blue-500">Home</a></li>
        <li><a href="/image_portfolio/timeline" class="text-blue-600 font-semibold">Timeline</a></li>
        <li><a href="/image_portfolio/map" class="text-gray-700 hover:text-blue-500">Map</a></li>
      </ul>
    </div>
  </nav>

  <!-- 📸 Timeline / Gallery -->
  <main class="max-w-6xl mx-auto px-4 py-8">
    <h1 class="text-3xl font-bold mb-6 text-center">{{ title }}</h1>

    <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-6">
  {% for image in timeline %}
  <div class="relative rounded overflow-hidden shadow bg-white">
    <img src="{{ image.thumb.m }}" alt="{{ image.filename }}" class="w-full h-64 object-cover">
    <div class="absolute bottom-0 left-0 right-0 bg-black bg-opacity-50 text-white text-sm p-2">
      {{ image.upload_date|date('d.m.Y H:i') }}
    </div>
    {% if image.title or image.description %}
    <div class="p-3">
      {% if image.title %}<h3 class="font-semibold text-lg">{{ image.title }}</h3>{% endif %}
      {% if image.description %}<p class="text-sm text-gray-700">{{ image.description }}</p>{% endif %}
    </div>
    {% endif %}
  </div>
  {% endfor %}
</div>
  </main>

</body>
</html>

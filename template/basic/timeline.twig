<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8">
  <title>{{ title }}</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 text-gray-900">

  <!-- 🧭 Fixed Instagram-style Nav -->
  <nav class="bg-white shadow-sm sticky top-0 z-50 border-b border-gray-200">
    <div class="max-w-6xl mx-auto px-4 py-3 flex justify-between items-center">
      <span class="text-2xl font-bold text-pink-600 tracking-tight">{{ site_title }}</span>
      <ul class="flex space-x-6 text-sm font-medium">
        <li><a href="/image_portfolio/home" class="text-gray-600 hover:text-pink-600">Home</a></li>
        <li><a href="/image_portfolio/timeline" class="text-pink-600">Timeline</a></li>
        <li><a href="/image_portfolio/map" class="text-gray-600 hover:text-pink-600">Map</a></li>
      </ul>
    </div>
  </nav>

  <!-- 🖼️ Instagram-style Feed -->
  <main class="max-w-6xl mx-auto px-4 py-8">
    <h1 class="text-3xl font-bold mb-6 text-center">{{ title }}</h1>

    <div class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 gap-2">
      {% for image in timeline %}
      <div class="relative group aspect-square bg-gray-200 overflow-hidden">
        <img 
          src="{{ image.thumb.m }}" 
          alt="{{ image.filename }}" 
          class="w-full h-full object-cover transition duration-300 ease-in-out group-hover:scale-105"
        >
        <div class="absolute inset-0 bg-black bg-opacity-40 opacity-0 group-hover:opacity-100 flex items-center justify-center transition">
          {% if image.exif.Date %}
            <p class="text-white text-xs">{{ image.exif.Date|date("d.m.Y H:i") }}</p>
          {% endif %}
        </div>
      </div>
      {% endfor %}
    </div>
  </main>

</body>
</html>
